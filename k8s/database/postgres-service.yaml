apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: securekube-app
spec:
  selector:
    app: securekube-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
