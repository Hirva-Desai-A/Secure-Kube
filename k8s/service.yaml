apiVersion: v1
kind: Service
metadata:
  name: securekube-api-service
  labels:
    app: securekube-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: securekube-api